##############################################################
# GAMMA-GAMMA SUBMODEL (MONETARY MODEL)
##############################################################

Gamma-Gamma Submodel, BG/NBD modelinin tamamlayıcı bir parçasıdır ve müşterilerin yaptığı işlemlerin parasal değerini
(monetary value) tahmin etmek için kullanılır.

BG/NBD modeli, bir müşterinin gelecekte kaç işlem yapacağını (transaction frequency) tahmin ederken;
Gamma-Gamma Submodel, bu işlemlerin ortalama değerini (average monetary value) tahmin eder.

Bu modelin temel varsayımı, bir müşterinin işlem başına ortalama getirisinin, yani transaction value’sunun,
müşteriden müşteriye farklılık gösterdiği ancak tek bir müşteri için zaman içerisinde sabit kaldığıdır.

------------------------------------------------------------
🔹 Temel Varsayımlar
------------------------------------------------------------

1. Her bir müşterinin ortalama işlem değeri (mean transaction value) zaman içinde sabittir.
2. Ancak farklı müşterilerin ortalama işlem değerleri birbirinden farklıdır.
3. Tüm müşterilerin ortalama işlem değerleri (monetary value) Gamma dağılımı gösterir.

Bu durumda:
    Transaction Value (V) ~ Gamma(p, q)

Burada:
- V : Bir müşterinin işlem başına ortalama harcaması
- p : Şekil (shape) parametresi
- q : Ölçek (scale) parametresi

------------------------------------------------------------
🔹 Modelin Amacı
------------------------------------------------------------

Gamma-Gamma Submodel, bir müşterinin işlem başına beklenen ortalama kârını tahmin etmek için kullanılır.

CLTV’nin parasal kısmı şu şekilde ifade edilir:

    Expected Monetary Value = E[X] = (p + x̄ * n) / (q + n)

ve toplam CLTV hesaplamasında kullanılır:

    CLTV = Expected Number of Transaction × Expected Average Profit

veya birleşik formda:

    CLTV = BG/NBD Model × Gamma-Gamma Submodel

------------------------------------------------------------
🔹 Modelin Matematiksel Formülü
------------------------------------------------------------

Bir müşterinin gelecekteki beklenen ortalama işlem değeri aşağıdaki formülle tahmin edilir:

    E(M | x̄, n) = (p + n) * x̄ / (q + n - 1)

Burada:
- E(M | x̄, n) : n adet gözlemden sonra müşterinin beklenen ortalama işlem değeri
- x̄ : Gözlemlenen ortalama işlem değeri (mean observed transaction value)
- n : Gözlemlenen işlem sayısı
- p, q : Gamma dağılım parametreleri

Bu formül, bir müşterinin geçmişteki ortalama işlem değerine ve işlem sayısına bakarak gelecekteki ortalama parasal
katkısının tahmin edilmesini sağlar.

------------------------------------------------------------
🔹 CLTV Hesaplamasında Rolü
------------------------------------------------------------

Gamma-Gamma Submodel, BG/NBD modelinin çıktısı olan "beklenen işlem sayısı" ile birleştirilerek toplam müşteri yaşam boyu
değerini (CLTV) tahmin eder.

    CLTV = BG/NBD (Expected Transactions) × Gamma-Gamma (Expected Monetary Value)

Bu kombinasyon, hem müşterinin gelecekte ne kadar sıklıkla alışveriş yapacağını hem de bu alışverişlerin parasal değerini
dikkate alarak gerçekçi bir CLTV tahmini sunar.

------------------------------------------------------------
🔹 Sonuç
------------------------------------------------------------

Gamma-Gamma Submodel, CLTV analizinin parasal tarafını modelleyen güçlü bir istatistiksel yaklaşımdır. BG/NBD’nin tahmin
ettiği işlem sıklığı ile birleştiğinde, işletmeler müşterilerinin gelecekteki toplam değerini daha doğru ve istatistiksel
olarak sağlam biçimde tahmin edebilir.